/**
 * @deprecated Ce fichier est maintenu uniquement pour la compatibilité descendante.
 * `Deces` a été déplacé et enrichi dans `events.model.ts` avec `typeAnimal` et `cycleId`.
 *
 * Migrer vers `import { Deces } from './events.model'`.
 */
export type { Deces } from './events.model';
